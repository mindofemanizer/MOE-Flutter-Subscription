name: moe_flutter_subscription
description: Subscription management for MOE Flutter ecosystem — plans, billing cycles.
version: 1.0.0
repository: https://github.com/mindofemanizer/MOE-Flutter-Subscription

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dio: ^5.7.0
  moe_flutter_core:
    git:
      url: https://github.com/mindofemanizer/MOE-Flutter-Core.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.12

topics:
  - subscription
  - billing
  - plan

